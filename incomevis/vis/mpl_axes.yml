x_config = {
    'name': 'x',
    'lim': [-70000, 40000],
    'ticks' : [-60000, -40000, -20000, 0, 20000, 40000],
    'tickslabel' : [-60000, -40000, -20000, 0, 20000, 40000],
    'label' : 'Distance from benchmark ($)',
    'labelpad' : 60,
    'pad' : 15
}

y_config = {
    'name': 'y',
    'lim': [0, len(segments)+1],
    'ticks' : [0,3,7,10],
    'tickslabel' : [getDecile()[i] for i in y_config['ticks']],
    'label' : 'Percentile',
    'labelpad' : 35,
    'pad' : 30
}

z_config = {
    'name': 'z',
    'lim': [0, 300000],
    'ticks' : [0,100000,200000,300000],
    'tickslabel' : [0,100000,200000,300000],
    'label' : 'Adjusted annual household income ($)',
    'labelpad' : 50,
    'pad' : 30
}

# # x lim, ticks, ticklabels, label params, 
# ax.set_xlim(xlim[0], xlim[1])
# ax.set_xticks(xticks)
# ax.set_xticklabels(xtickslabel, fontsize=15, fontweight='bold')
# ax.set_xlabel(xlabel, fontweight = 'bold', labelpad = xlabelpad, fontsize = 20)
# ax.tick_params(axis = 'x', which = 'major', pad = xpad)

# # y lim, ticks, ticklabels, label, params
# ax.set_ylim3d(ylim[0], ylim[1])
# ax.set_yticks(yticks)
# ax.set_yticklabels(ytickslabel,fontsize=15, fontweight = 'bold') 
# ax.set_ylabel(ylabel, fontweight = 'bold', labelpad = ylabelpad, fontsize = 20)
# ax.tick_params(axis = 'y', which = 'major', pad = ypad)

# # z lim, ticks, ticklabels, label, params
# ax.set_zlim(zlim[0], zlim[1]) 
# ax.set_zticks(zticks)
# ax.set_zticklabels(ztickslabel,fontsize=15, fontweight='bold')
# ax.set_zlabel(zlabel, fontweight = 'bold', labelpad = zlabelpad, fontsize = 20, rotation = 90)
# ax.tick_params(axis = 'z', which = 'major', pad = zpad)